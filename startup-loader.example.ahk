#Requires AutoHotkey v2.0

; Replace YOUR_USERNAME with your Windows user name, then copy this loader
; into the Windows Startup folder (open it with Win+R, then shell:startup).
#Include C:\Users\YOUR_USERNAME\.config\autohotkey\capslock-ime-safe-mouse.ahk
